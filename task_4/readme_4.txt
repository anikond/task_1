Функция f(n) записывает число в обратном порядке. При этом лидирующие нули будут удаляться.
Функция g(n) определяется как g(n) = f(f(n))/n.

f(n) - переворачивает число и убирает лидирующие нули;
f(f(n)) - снова перевернет число.

Можно заменить, что в функции g(n) добавление нулей в конец числа n будет уменьшать результат в 10 раз,
так как f(f(n)) вернет число m, которое будет являться n, но без нулей в конце, и m будет делится на n,
которое по сути представляет собой m * 10**k:

g(n) = m/(m * 10**k), где 10**k представляет количество нулей в конце числа n.

Исходя из условия 1 < n < 10**30, число n может иметь до 29 нулей и еще добавляется деление на само число n,
которое даст 1. 
Следовательно, функция 𝑔(𝑛)  принимает 30 различных значений.  

Ответ на задачу: 30